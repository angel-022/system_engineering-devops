#!/usr/bin/env bash
# This script lists all running processes on the system, including those without TTY,
# and shows the process hierarchy in a user-readable format.

ps -eo user,pid,ppid,%cpu,%mem,vsz,rss,tty,stat,start,time,cmd --forest
